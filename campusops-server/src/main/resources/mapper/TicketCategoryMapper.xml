<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.campusops.mapper.TicketCategoryMapper">

    <select id="selectEnabledById" resultType="com.campusops.entity.TicketCategoryEntity">
        SELECT id, category_code, category_name, description, parent_id, enabled, created_at, updated_at, deleted
        FROM ticket_category
        WHERE id = #{id} AND enabled = 1 AND deleted = 0
    </select>

</mapper>